# Compose renderer contract

The renderer consumes the pinned `ocis_full` example. It selects supported overlays and emits stable
references to a separately generated secret payload; it does not fork image/version defaults into UI code.

Docker is the first production-oriented path. Podman stays Community Preview until its promotion matrix
passes. Collabora is the only office overlay.
