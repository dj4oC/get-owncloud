---
dependency:
  name: galaxy
driver:
  name: default
platforms:
  - name: localhost
provisioner:
  name: ansible
  inventory:
    host_vars:
      localhost:
        ansible_connection: local
        owncloud_bundle_dir: "{{ lookup('env', 'GET_OWNCLOUD_E2E_BUNDLE') }}"
        owncloud_accept_eula: true
verifier:
  name: ansible
